<h1>Edit Profile</h1>

<%= form_with model: @profile, local: true do |f| %>
  <div>
    <%= f.label :bio %>
    <%= f.text_area :bio %>
  </div>

  <div>
    <%= f.submit 'Update Profile' %>
  </div>
<% end %>
